@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    //using (Html.BeginForm("Login", "Usuario", FormMethod.Post, new { id = "logoutForm", @class = "nav-link" }))
    //{
    @*@Html.AntiForgeryToken()*@

    <ul class="nav navbar-nav" style=" margin-left: 73%;">
        <li>
            @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage", @class = "nav-link" })
        </li>
        <li>
            @Html.ActionLink("Cerrar Sesion", "LogOff", "Usuario", null, new { @class = "nav-link" })
        </li>
    </ul>
    //}
}
else
{
    <ul class="nav navbar-nav" style=" margin-left: 73%;">
        <li>@Html.ActionLink("Register", "SignUp", "Usuario", routeValues: null, htmlAttributes: new { id = "registerLink", @class = "nav-link" })</li>
        <li>@Html.ActionLink("Log in", "Login", "Usuario", routeValues: null, htmlAttributes: new { id = "loginLink", @class = "nav-link" })</li>
    </ul>
}
